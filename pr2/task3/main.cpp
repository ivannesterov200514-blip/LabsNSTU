#include "invmod.h"

int main() {
    setlocale(LC_ALL, "rus");
    int64_t c, m;
    cout << "Введите основание и модуль: ";
    cin >> c >> m;
    cout << "\n" << c << "^(-1) mod " << m << " = d\nИщем обратный элемент\n";

    cout << "\nr - остаток от деления;\nd - коэффициент при " << c << "; v - коэффициент при " << m << "; q - целая часть от деления\n";
    int64_t d = check(c, m);
    if (d != -1) {
        cout << "\nd (в строке с последним ненулевым остатком) = " << d << "\n";
        cout << "Проверка: " << c << " * " << d << " mod " << m << " = " << (c * d) % m << "\n";
    }
    return 0;
}
